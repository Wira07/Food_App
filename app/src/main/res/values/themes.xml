<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Food_App" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/sage</item>
        <item name="colorPrimaryDark">@color/sage</item>
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Food_App" parent="Base.Theme.Food_App" />

    <!-- Customized hint text appearance -->
    <style name="MyHintTextAppearance" parent="TextAppearance.Design.Hint">
        <item name="android:textSize">10sp</item> <!-- Ukuran teks hint -->
    </style>
</resources>